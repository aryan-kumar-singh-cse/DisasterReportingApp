{
  "name": "disaster-reporting-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm --prefix frontend test",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
